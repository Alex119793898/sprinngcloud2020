server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      # 访问 linux 配置的 nacos
      discovery:
        server-addr: 10.211.55.14:80
      config:
        server-addr: 10.211.55.14:80
        file-extension: yaml
        group: DEFAULT_GROUP
#  cloud:
#    nacos:
#      discovery:
#        server-addr: localhost:8848 #nacos 服务注册中心
#      config:
#        server-addr: localhost:8848  #nacos 配置中心地址
#        file-extension: yaml        #指定yaml格式的配置
#        group: DEFAULT_GROUP
#        namespace: b46f3338-224f-4f06-81c6-1fa79c3e61db
